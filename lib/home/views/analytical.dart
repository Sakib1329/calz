import 'package:flutter/cupertino.dart';

class Analytical extends StatelessWidget {
  const Analytical({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Analytical');
  }
}
